function fizzBuzz(num) {
    let i  = 1;
    while (i < num) {
        for (let i%0, i++ ) {q
            console.log(Fizz)
        }
        else if (i % 5 === 0) {
            console.log(Buzz)
        }
    }
    return 
}
